{{- if index .Values.config "basyx-infra.yml" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aas-web-ui.fullname" . }}-basyx-infra-config
  labels:
    {{- include "aas-web-ui.labels" . | nindent 4 }}
data:
  basyx-infra.yml: |
{{- index .Values.config "basyx-infra.yml" | nindent 4 }}
{{ end -}}
